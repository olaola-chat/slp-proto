// ==========================================================================
// This is auto-generated by gf cli tool. DO NOT EDIT THIS FILE MANUALLY.
// ==========================================================================

package internal

import (
	"context"
	"database/sql"
	"errors"
	"reflect"
	"strings"
	"time"

	"github.com/gogf/gf/container/gvar"
	"github.com/gogf/gf/database/gdb"
	"github.com/gogf/gf/frame/g"
	"github.com/gogf/gf/frame/gmvc"
	"github.com/gogf/gf/util/gconv"

	//	"banban/app/model"
	"banban/app/pb"
)

// XsChatroomExtendUnderDao is the manager for logic model data accessing
// and custom defined data operations functions management.
type XsChatroomExtendUnderDao struct {
	gmvc.M
	DB      gdb.DB
	Table   string
	Columns xsChatroomExtendUnderColumns
}

// XsChatroomExtendUnderColumns defines and stores column names for table xs_chatroom_extend_under.
type xsChatroomExtendUnderColumns struct {
	Rid              string //
	Vvc              string // 每次游戏结束，+1
	State            string // 等待,分配角色,准备发言,发言,投票,投票结果,pk,死亡结果,公布死亡,游戏结果,结束
	PopulaceText     string // 平民词语, 不要把此数据输出了
	UnderText        string // 卧底词语, 不要把此数据输出了
	Count            string // 第几轮
	Strict           string // 是否严格模式
	Ispk             string // 是否pk环节
	Pklist           string // pk列表
	Start            string // 首次发言开始位置
	Current          string // 当前发言位序
	Turns            string // 此轮发言顺序，当数据为空时，此轮结束
	Dateline         string // 状态变化的开始时间，包括计时变化
	Version          string //
	Admin            string // 娱乐模式管理uid，如果未设置则默认是0号麦
	ServerVersion    string // 服务端版本号
	VoteResult       string // 投票结果状态:1-全部弃票,2-无投票结果,3-pk,4-有投票结果
	KickNum          string // 被踢+被下麦人数
	EnterNum         string // 进房人数
	ForbiddenRecruit string //
}

var (
	// XsChatroomExtendUnder is globally public accessible object for table xs_chatroom_extend_under operations.
	XsChatroomExtendUnder = XsChatroomExtendUnderDao{
		M:     g.DB("default").Model("xs_chatroom_extend_under").Safe(),
		DB:    g.DB("default"),
		Table: "xs_chatroom_extend_under",
		Columns: xsChatroomExtendUnderColumns{
			Rid:              "rid",
			Vvc:              "vvc",
			State:            "state",
			PopulaceText:     "populace_text",
			UnderText:        "under_text",
			Count:            "count",
			Strict:           "strict",
			Ispk:             "ispk",
			Pklist:           "pklist",
			Start:            "start",
			Current:          "current",
			Turns:            "turns",
			Dateline:         "dateline",
			Version:          "version",
			Admin:            "admin",
			ServerVersion:    "server_version",
			VoteResult:       "vote_result",
			KickNum:          "kickNum",
			EnterNum:         "enterNum",
			ForbiddenRecruit: "forbiddenRecruit",
		},
	}
)

// Ctx is a chaining function, which creates and returns a new DB that is a shallow copy
// of current DB object and with given context in it.
// Note that this returned DB object can be used only once, so do not assign it to
// a global or package variable for long using.
func (d *XsChatroomExtendUnderDao) Ctx(ctx context.Context) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Ctx(ctx)}
}

// As sets an alias name for current table.
func (d *XsChatroomExtendUnderDao) As(as string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.As(as)}
}

// TX sets the transaction for current operation.
func (d *XsChatroomExtendUnderDao) TX(tx *gdb.TX) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.TX(tx)}
}

// Master marks the following operation on master node.
func (d *XsChatroomExtendUnderDao) Master() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Master()}
}

// Slave marks the following operation on slave node.
// Note that it makes sense only if there's any slave node configured.
func (d *XsChatroomExtendUnderDao) Slave() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Slave()}
}

// Args sets custom arguments for model operation.
func (d *XsChatroomExtendUnderDao) Args(args ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Args(args...)}
}

// LeftJoin does "LEFT JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").LeftJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").LeftJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsChatroomExtendUnderDao) LeftJoin(table ...string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.LeftJoin(table...)}
}

// RightJoin does "RIGHT JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").RightJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").RightJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsChatroomExtendUnderDao) RightJoin(table ...string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.RightJoin(table...)}
}

// InnerJoin does "INNER JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").InnerJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").InnerJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsChatroomExtendUnderDao) InnerJoin(table ...string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.InnerJoin(table...)}
}

// Fields sets the operation fields of the model, multiple fields joined using char ','.
// The parameter <fieldNamesOrMapStruct> can be type of string/map/*map/struct/*struct.
func (d *XsChatroomExtendUnderDao) Fields(fieldNamesOrMapStruct ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Fields(fieldNamesOrMapStruct...)}
}

// FieldsEx sets the excluded operation fields of the model, multiple fields joined using char ','.
// The parameter <fieldNamesOrMapStruct> can be type of string/map/*map/struct/*struct.
func (d *XsChatroomExtendUnderDao) FieldsEx(fieldNamesOrMapStruct ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.FieldsEx(fieldNamesOrMapStruct...)}
}

// Option sets the extra operation option for the model.
func (d *XsChatroomExtendUnderDao) Option(option int) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Option(option)}
}

// OmitEmpty sets OPTION_OMITEMPTY option for the model, which automatically filers
// the data and where attributes for empty values.
func (d *XsChatroomExtendUnderDao) OmitEmpty() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.OmitEmpty()}
}

// Filter marks filtering the fields which does not exist in the fields of the operated table.
func (d *XsChatroomExtendUnderDao) Filter() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Filter()}
}

// Where sets the condition statement for the model. The parameter <where> can be type of
// string/map/gmap/slice/struct/*struct, etc. Note that, if it's called more than one times,
// multiple conditions will be joined into where statement using "AND".
// Eg:
// Where("uid=10000")
// Where("uid", 10000)
// Where("money>? AND name like ?", 99999, "vip_%")
// Where("uid", 1).Where("name", "john")
// Where("status IN (?)", g.Slice{1,2,3})
// Where("age IN(?,?)", 18, 50)
// Where(User{ Id : 1, UserName : "john"})
func (d *XsChatroomExtendUnderDao) Where(where interface{}, args ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Where(where, args...)}
}

// WherePri does the same logic as M.Where except that if the parameter <where>
// is a single condition like int/string/float/slice, it treats the condition as the primary
// key value. That is, if primary key is "id" and given <where> parameter as "123", the
// WherePri function treats the condition as "id=123", but M.Where treats the condition
// as string "123".
func (d *XsChatroomExtendUnderDao) WherePri(where interface{}, args ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.WherePri(where, args...)}
}

// And adds "AND" condition to the where statement.
func (d *XsChatroomExtendUnderDao) And(where interface{}, args ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.And(where, args...)}
}

// Or adds "OR" condition to the where statement.
func (d *XsChatroomExtendUnderDao) Or(where interface{}, args ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Or(where, args...)}
}

// Group sets the "GROUP BY" statement for the model.
func (d *XsChatroomExtendUnderDao) Group(groupBy string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Group(groupBy)}
}

// Order sets the "ORDER BY" statement for the model.
func (d *XsChatroomExtendUnderDao) Order(orderBy ...string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Order(orderBy...)}
}

// Limit sets the "LIMIT" statement for the model.
// The parameter <limit> can be either one or two number, if passed two number is passed,
// it then sets "LIMIT limit[0],limit[1]" statement for the model, or else it sets "LIMIT limit[0]"
// statement.
func (d *XsChatroomExtendUnderDao) Limit(limit ...int) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Limit(limit...)}
}

// Offset sets the "OFFSET" statement for the model.
// It only makes sense for some databases like SQLServer, PostgreSQL, etc.
func (d *XsChatroomExtendUnderDao) Offset(offset int) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Offset(offset)}
}

// Page sets the paging number for the model.
// The parameter <page> is started from 1 for paging.
// Note that, it differs that the Limit function start from 0 for "LIMIT" statement.
func (d *XsChatroomExtendUnderDao) Page(page, limit int) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Page(page, limit)}
}

// Batch sets the batch operation number for the model.
func (d *XsChatroomExtendUnderDao) Batch(batch int) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Batch(batch)}
}

// Cache sets the cache feature for the model. It caches the result of the sql, which means
// if there's another same sql request, it just reads and returns the result from cache, it
// but not committed and executed into the database.
//
// If the parameter <duration> < 0, which means it clear the cache with given <name>.
// If the parameter <duration> = 0, which means it never expires.
// If the parameter <duration> > 0, which means it expires after <duration>.
//
// The optional parameter <name> is used to bind a name to the cache, which means you can later
// control the cache like changing the <duration> or clearing the cache with specified <name>.
//
// Note that, the cache feature is disabled if the model is operating on a transaction.
func (d *XsChatroomExtendUnderDao) Cache(duration time.Duration, name ...string) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Cache(duration, name...)}
}

// Data sets the operation data for the model.
// The parameter <data> can be type of string/map/gmap/slice/struct/*struct, etc.
// Eg:
// Data("uid=10000")
// Data("uid", 10000)
// Data(g.Map{"uid": 10000, "name":"john"})
// Data(g.Slice{g.Map{"uid": 10000, "name":"john"}, g.Map{"uid": 20000, "name":"smith"})
func (d *XsChatroomExtendUnderDao) Data(data ...interface{}) *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Data(data...)}
}

// All does "SELECT FROM ..." statement for the model.
// It retrieves the records from table and returns the result as []*pb.EntityXsChatroomExtendUnder.
// It returns nil if there's no record retrieved with the given conditions from table.
//
// The optional parameter <where> is the same as the parameter of M.Where function,
// see M.Where.
func (d *XsChatroomExtendUnderDao) All(where ...interface{}) ([]*pb.EntityXsChatroomExtendUnder, error) {
	all, err := d.M.All(where...)
	if err != nil {
		return nil, err
	}
	var entities []*pb.EntityXsChatroomExtendUnder
	//if err = all.Structs(&entities); err != nil && err != sql.ErrNoRows {
	//	return nil, err
	//}
	if err = d.scan(all, &entities); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entities, nil
}

// One retrieves one record from table and returns the result as *pb.EntityXsChatroomExtendUnder.
// It returns nil if there's no record retrieved with the given conditions from table.
//
// The optional parameter <where> is the same as the parameter of M.Where function,
// see M.Where.
func (d *XsChatroomExtendUnderDao) One(where ...interface{}) (*pb.EntityXsChatroomExtendUnder, error) {
	one, err := d.M.One(where...)
	if err != nil {
		return nil, err
	}
	var entity *pb.EntityXsChatroomExtendUnder
	if err = one.Struct(&entity); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entity, nil
}

// FindOne retrieves and returns a single Record by M.WherePri and M.One.
// Also see M.WherePri and M.One.
func (d *XsChatroomExtendUnderDao) FindOne(where ...interface{}) (*pb.EntityXsChatroomExtendUnder, error) {
	one, err := d.M.FindOne(where...)
	if err != nil {
		return nil, err
	}
	var entity *pb.EntityXsChatroomExtendUnder
	if err = one.Struct(&entity); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entity, nil
}

// FindAll retrieves and returns Result by by M.WherePri and M.All.
// Also see M.WherePri and M.All.
func (d *XsChatroomExtendUnderDao) FindAll(where ...interface{}) ([]*pb.EntityXsChatroomExtendUnder, error) {
	all, err := d.M.FindAll(where...)
	if err != nil {
		return nil, err
	}
	var entities []*pb.EntityXsChatroomExtendUnder
	//if err = all.Structs(&entities); err != nil && err != sql.ErrNoRows {
	//	return nil, err
	//}
	if err = d.scan(all, &entities); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entities, nil
}

// Struct retrieves one record from table and converts it into given struct.
// The parameter <pointer> should be type of *struct/**struct. If type **struct is given,
// it can create the struct internally during converting.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved with the given conditions
// from table and <pointer> is not nil.
//
// Eg:
// user := new(User)
// err  := dao.User.Where("id", 1).Struct(user)
//
// user := (*User)(nil)
// err  := dao.User.Where("id", 1).Struct(&user)
func (d *XsChatroomExtendUnderDao) Struct(pointer interface{}, where ...interface{}) error {
	return d.M.Struct(pointer, where...)
}

// Structs retrieves records from table and converts them into given struct slice.
// The parameter <pointer> should be type of *[]struct/*[]*struct. It can create and fill the struct
// slice internally during converting.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved with the given conditions
// from table and <pointer> is not empty.
//
// Eg:
// users := ([]User)(nil)
// err   := dao.User.Structs(&users)
//
// users := ([]*User)(nil)
// err   := dao.User.Structs(&users)
func (d *XsChatroomExtendUnderDao) Structs(pointer interface{}, where ...interface{}) error {
	return d.M.Structs(pointer, where...)
}

// Scan automatically calls Struct or Structs function according to the type of parameter <pointer>.
// It calls function Struct if <pointer> is type of *struct/**struct.
// It calls function Structs if <pointer> is type of *[]struct/*[]*struct.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved and given pointer is not empty or nil.
//
// Eg:
// user  := new(User)
// err   := dao.User.Where("id", 1).Scan(user)
//
// user  := (*User)(nil)
// err   := dao.User.Where("id", 1).Scan(&user)
//
// users := ([]User)(nil)
// err   := dao.User.Scan(&users)
//
// users := ([]*User)(nil)
// err   := dao.User.Scan(&users)
func (d *XsChatroomExtendUnderDao) Scan(pointer interface{}, where ...interface{}) error {
	return d.M.Scan(pointer, where...)
}

// Chunk iterates the table with given size and callback function.
func (d *XsChatroomExtendUnderDao) Chunk(limit int, callback func(entities []*pb.EntityXsChatroomExtendUnder, err error) bool) {
	d.M.Chunk(limit, func(result gdb.Result, err error) bool {
		var entities []*pb.EntityXsChatroomExtendUnder
		err = result.Structs(&entities)
		if err == sql.ErrNoRows {
			return false
		}
		return callback(entities, err)
	})
}

// LockUpdate sets the lock for update for current operation.
func (d *XsChatroomExtendUnderDao) LockUpdate() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.LockUpdate()}
}

// LockShared sets the lock in share mode for current operation.
func (d *XsChatroomExtendUnderDao) LockShared() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.LockShared()}
}

// Unscoped enables/disables the soft deleting feature.
func (d *XsChatroomExtendUnderDao) Unscoped() *XsChatroomExtendUnderDao {
	return &XsChatroomExtendUnderDao{M: d.M.Unscoped()}
}

func (d *XsChatroomExtendUnderDao) scan(res gdb.Result, in interface{}) error {
	length := res.Len()
	if length == 0 {
		return sql.ErrNoRows
	}
	v := reflect.ValueOf(in).Elem()
	newv := reflect.MakeSlice(v.Type(), 0, length)
	v.Set(newv)
	v.SetLen(length)

	index := 0
	var fieldToTag []string
	for i := 0; i < length; i++ {
		k := v.Type().Elem().Elem()
		newObj := reflect.New(k)
		if i == 0 {
			fieldToTag = d.getFieldTag(newObj)
		}
		err := d.mapping(res[i], newObj, fieldToTag)
		if err != nil {
			return err
		}
		v.Index(index).Set(newObj)
		index++
	}
	v.SetLen(index)
	return nil
}

func (d *XsChatroomExtendUnderDao) getFieldTag(v reflect.Value) []string {
	t := v.Type()
	typ := t.Elem()
	val := v.Elem()
	vTypeOfT := val.Type()
	length := val.NumField()
	fieldToTag := make([]string, length)
	for j := 0; j < length; j++ {
		name := vTypeOfT.Field(j).Name
		if name[0] < 'A' || name[0] > 'Z' {
			continue
		}
		value := val.Field(j)
		tagVal := typ.Field(j).Tag.Get("orm")
		if !value.CanSet() || len(tagVal) == 0 {
			continue
		}
		index := strings.Index(tagVal, ",")
		if index > -1 {
			fieldToTag[j] = tagVal[0:index]
		} else {
			fieldToTag[j] = tagVal
		}
	}
	return fieldToTag
}

func (d *XsChatroomExtendUnderDao) mapping(m map[string]*gvar.Var, v reflect.Value, fieldToTag []string) error {
	val := v.Elem()
	length := val.NumField()
	for i := 0; i < length; i++ {
		tag := fieldToTag[i]
		if len(tag) == 0 {
			continue
		}

		meta, ok := m[tag]
		if !ok {
			continue
		}
		value := val.Field(i)
		kind := value.Kind()
		switch kind {
		case reflect.String:
			value.SetString(meta.String())
			break
		case reflect.Float32, reflect.Float64:
			value.SetFloat(gconv.Float64(meta.String()))
			break
		case reflect.Int64, reflect.Int, reflect.Int8, reflect.Int16, reflect.Int32:
			value.SetInt(meta.Int64())
			break
		case reflect.Uint64, reflect.Uint, reflect.Uint8, reflect.Uint16, reflect.Uint32:
			value.SetUint(meta.Uint64())
			break
		case reflect.Bool:
			value.SetBool(meta.Bool())
			break
		default:
			return errors.New("unknown kind " + kind.String())
			break
		}
	}
	return nil
}
