// ==========================================================================
// This is auto-generated by gf cli tool. DO NOT EDIT THIS FILE MANUALLY.
// ==========================================================================

package internal

import (
	"context"
	"database/sql"
	"errors"
	"reflect"
	"strings"
	"time"

	"github.com/gogf/gf/container/gvar"
	"github.com/gogf/gf/database/gdb"
	"github.com/gogf/gf/frame/g"
	"github.com/gogf/gf/frame/gmvc"

	//	"banban/app/model"
	"banban/app/pb"
)

// XsChatroomModuleFactoryDao is the manager for logic model data accessing
// and custom defined data operations functions management.
type XsChatroomModuleFactoryDao struct {
	gmvc.M
	DB      gdb.DB
	Table   string
	Columns xsChatroomModuleFactoryColumns
}

// XsChatroomModuleFactoryColumns defines and stores column names for table xs_chatroom_module_factory.
type xsChatroomModuleFactoryColumns struct {
	ID                    string //
	FactoryName           string // 房间工厂名称
	FactoryType           string // 房间工厂类型
	ModuleID              string // 使用的房间模版id
	PartyType             string // 隶属于哪个party
	DefaultPaier          string // 是否显示老板位置
	DefaultReception      string // 是否显示接待位
	DefaultDisplayMessage string // 是否打开公屏
	DefaultTheme          string // 皮肤
	DefaultBackground     string // 背景图
	DefaultCounter        string // 是否使用计时器
	DefaultMode           string // 是否自由上麦
	DefaultNine           string // 是否使用9麦,即不显示房主位
	DefaultState          string // //房间私密等级 ：公开 const ROOM_PRIVATE_LEVEL_PUBLIC =0; //房间私密等级:私人 const ROOM_PRIVATE_LEVEL_PERSON =1;    //房间私密等级：仅好友可见    const ROOM_PRIVATE_LEVEL_FRIEND = 2;    //房间私密登记：仅粉丝可见    const ROOM_PRIVATE_LEVEL_FANS = 3;
	DefaultTagID          string //
	PromiseApp            string // 允许的app应用
	PromiseProperty       string // 允许的房间属性
	BackendDefault        string // 是否是作为运营后台的标准模版 0-不是运营后台的标准模版 1-是运营后台的标准模版
}

var (
	// XsChatroomModuleFactory is globally public accessible object for table xs_chatroom_module_factory operations.
	XsChatroomModuleFactory = XsChatroomModuleFactoryDao{
		M:     g.DB("default").Model("xs_chatroom_module_factory").Safe(),
		DB:    g.DB("default"),
		Table: "xs_chatroom_module_factory",
		Columns: xsChatroomModuleFactoryColumns{
			ID:                    "id",
			FactoryName:           "factory_name",
			FactoryType:           "factory_type",
			ModuleID:              "module_id",
			PartyType:             "party_type",
			DefaultPaier:          "default_paier",
			DefaultReception:      "default_reception",
			DefaultDisplayMessage: "default_display_message",
			DefaultTheme:          "default_theme",
			DefaultBackground:     "default_background",
			DefaultCounter:        "default_counter",
			DefaultMode:           "default_mode",
			DefaultNine:           "default_nine",
			DefaultState:          "default_state",
			DefaultTagID:          "default_tag_id",
			PromiseApp:            "promise_app",
			PromiseProperty:       "promise_property",
			BackendDefault:        "backend_default",
		},
	}
)

// Ctx is a chaining function, which creates and returns a new DB that is a shallow copy
// of current DB object and with given context in it.
// Note that this returned DB object can be used only once, so do not assign it to
// a global or package variable for long using.
func (d *XsChatroomModuleFactoryDao) Ctx(ctx context.Context) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Ctx(ctx)}
}

// As sets an alias name for current table.
func (d *XsChatroomModuleFactoryDao) As(as string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.As(as)}
}

// TX sets the transaction for current operation.
func (d *XsChatroomModuleFactoryDao) TX(tx *gdb.TX) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.TX(tx)}
}

// Master marks the following operation on master node.
func (d *XsChatroomModuleFactoryDao) Master() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Master()}
}

// Slave marks the following operation on slave node.
// Note that it makes sense only if there's any slave node configured.
func (d *XsChatroomModuleFactoryDao) Slave() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Slave()}
}

// Args sets custom arguments for model operation.
func (d *XsChatroomModuleFactoryDao) Args(args ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Args(args...)}
}

// LeftJoin does "LEFT JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").LeftJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").LeftJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsChatroomModuleFactoryDao) LeftJoin(table ...string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.LeftJoin(table...)}
}

// RightJoin does "RIGHT JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").RightJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").RightJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsChatroomModuleFactoryDao) RightJoin(table ...string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.RightJoin(table...)}
}

// InnerJoin does "INNER JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").InnerJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").InnerJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsChatroomModuleFactoryDao) InnerJoin(table ...string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.InnerJoin(table...)}
}

// Fields sets the operation fields of the model, multiple fields joined using char ','.
// The parameter <fieldNamesOrMapStruct> can be type of string/map/*map/struct/*struct.
func (d *XsChatroomModuleFactoryDao) Fields(fieldNamesOrMapStruct ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Fields(fieldNamesOrMapStruct...)}
}

// FieldsEx sets the excluded operation fields of the model, multiple fields joined using char ','.
// The parameter <fieldNamesOrMapStruct> can be type of string/map/*map/struct/*struct.
func (d *XsChatroomModuleFactoryDao) FieldsEx(fieldNamesOrMapStruct ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.FieldsEx(fieldNamesOrMapStruct...)}
}

// Option sets the extra operation option for the model.
func (d *XsChatroomModuleFactoryDao) Option(option int) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Option(option)}
}

// OmitEmpty sets OPTION_OMITEMPTY option for the model, which automatically filers
// the data and where attributes for empty values.
func (d *XsChatroomModuleFactoryDao) OmitEmpty() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.OmitEmpty()}
}

// Filter marks filtering the fields which does not exist in the fields of the operated table.
func (d *XsChatroomModuleFactoryDao) Filter() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Filter()}
}

// Where sets the condition statement for the model. The parameter <where> can be type of
// string/map/gmap/slice/struct/*struct, etc. Note that, if it's called more than one times,
// multiple conditions will be joined into where statement using "AND".
// Eg:
// Where("uid=10000")
// Where("uid", 10000)
// Where("money>? AND name like ?", 99999, "vip_%")
// Where("uid", 1).Where("name", "john")
// Where("status IN (?)", g.Slice{1,2,3})
// Where("age IN(?,?)", 18, 50)
// Where(User{ Id : 1, UserName : "john"})
func (d *XsChatroomModuleFactoryDao) Where(where interface{}, args ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Where(where, args...)}
}

// WherePri does the same logic as M.Where except that if the parameter <where>
// is a single condition like int/string/float/slice, it treats the condition as the primary
// key value. That is, if primary key is "id" and given <where> parameter as "123", the
// WherePri function treats the condition as "id=123", but M.Where treats the condition
// as string "123".
func (d *XsChatroomModuleFactoryDao) WherePri(where interface{}, args ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.WherePri(where, args...)}
}

// And adds "AND" condition to the where statement.
func (d *XsChatroomModuleFactoryDao) And(where interface{}, args ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.And(where, args...)}
}

// Or adds "OR" condition to the where statement.
func (d *XsChatroomModuleFactoryDao) Or(where interface{}, args ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Or(where, args...)}
}

// Group sets the "GROUP BY" statement for the model.
func (d *XsChatroomModuleFactoryDao) Group(groupBy string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Group(groupBy)}
}

// Order sets the "ORDER BY" statement for the model.
func (d *XsChatroomModuleFactoryDao) Order(orderBy ...string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Order(orderBy...)}
}

// Limit sets the "LIMIT" statement for the model.
// The parameter <limit> can be either one or two number, if passed two number is passed,
// it then sets "LIMIT limit[0],limit[1]" statement for the model, or else it sets "LIMIT limit[0]"
// statement.
func (d *XsChatroomModuleFactoryDao) Limit(limit ...int) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Limit(limit...)}
}

// Offset sets the "OFFSET" statement for the model.
// It only makes sense for some databases like SQLServer, PostgreSQL, etc.
func (d *XsChatroomModuleFactoryDao) Offset(offset int) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Offset(offset)}
}

// Page sets the paging number for the model.
// The parameter <page> is started from 1 for paging.
// Note that, it differs that the Limit function start from 0 for "LIMIT" statement.
func (d *XsChatroomModuleFactoryDao) Page(page, limit int) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Page(page, limit)}
}

// Batch sets the batch operation number for the model.
func (d *XsChatroomModuleFactoryDao) Batch(batch int) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Batch(batch)}
}

// Cache sets the cache feature for the model. It caches the result of the sql, which means
// if there's another same sql request, it just reads and returns the result from cache, it
// but not committed and executed into the database.
//
// If the parameter <duration> < 0, which means it clear the cache with given <name>.
// If the parameter <duration> = 0, which means it never expires.
// If the parameter <duration> > 0, which means it expires after <duration>.
//
// The optional parameter <name> is used to bind a name to the cache, which means you can later
// control the cache like changing the <duration> or clearing the cache with specified <name>.
//
// Note that, the cache feature is disabled if the model is operating on a transaction.
func (d *XsChatroomModuleFactoryDao) Cache(duration time.Duration, name ...string) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Cache(duration, name...)}
}

// Data sets the operation data for the model.
// The parameter <data> can be type of string/map/gmap/slice/struct/*struct, etc.
// Eg:
// Data("uid=10000")
// Data("uid", 10000)
// Data(g.Map{"uid": 10000, "name":"john"})
// Data(g.Slice{g.Map{"uid": 10000, "name":"john"}, g.Map{"uid": 20000, "name":"smith"})
func (d *XsChatroomModuleFactoryDao) Data(data ...interface{}) *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Data(data...)}
}

// All does "SELECT FROM ..." statement for the model.
// It retrieves the records from table and returns the result as []*pb.EntityXsChatroomModuleFactory.
// It returns nil if there's no record retrieved with the given conditions from table.
//
// The optional parameter <where> is the same as the parameter of M.Where function,
// see M.Where.
func (d *XsChatroomModuleFactoryDao) All(where ...interface{}) ([]*pb.EntityXsChatroomModuleFactory, error) {
	all, err := d.M.All(where...)
	if err != nil {
		return nil, err
	}
	var entities []*pb.EntityXsChatroomModuleFactory
	//if err = all.Structs(&entities); err != nil && err != sql.ErrNoRows {
	//	return nil, err
	//}
	if err = d.scan(all, &entities); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entities, nil
}

// One retrieves one record from table and returns the result as *pb.EntityXsChatroomModuleFactory.
// It returns nil if there's no record retrieved with the given conditions from table.
//
// The optional parameter <where> is the same as the parameter of M.Where function,
// see M.Where.
func (d *XsChatroomModuleFactoryDao) One(where ...interface{}) (*pb.EntityXsChatroomModuleFactory, error) {
	one, err := d.M.One(where...)
	if err != nil {
		return nil, err
	}
	var entity *pb.EntityXsChatroomModuleFactory
	if err = one.Struct(&entity); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entity, nil
}

// FindOne retrieves and returns a single Record by M.WherePri and M.One.
// Also see M.WherePri and M.One.
func (d *XsChatroomModuleFactoryDao) FindOne(where ...interface{}) (*pb.EntityXsChatroomModuleFactory, error) {
	one, err := d.M.FindOne(where...)
	if err != nil {
		return nil, err
	}
	var entity *pb.EntityXsChatroomModuleFactory
	if err = one.Struct(&entity); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entity, nil
}

// FindAll retrieves and returns Result by by M.WherePri and M.All.
// Also see M.WherePri and M.All.
func (d *XsChatroomModuleFactoryDao) FindAll(where ...interface{}) ([]*pb.EntityXsChatroomModuleFactory, error) {
	all, err := d.M.FindAll(where...)
	if err != nil {
		return nil, err
	}
	var entities []*pb.EntityXsChatroomModuleFactory
	//if err = all.Structs(&entities); err != nil && err != sql.ErrNoRows {
	//	return nil, err
	//}
	if err = d.scan(all, &entities); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entities, nil
}

// Struct retrieves one record from table and converts it into given struct.
// The parameter <pointer> should be type of *struct/**struct. If type **struct is given,
// it can create the struct internally during converting.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved with the given conditions
// from table and <pointer> is not nil.
//
// Eg:
// user := new(User)
// err  := dao.User.Where("id", 1).Struct(user)
//
// user := (*User)(nil)
// err  := dao.User.Where("id", 1).Struct(&user)
func (d *XsChatroomModuleFactoryDao) Struct(pointer interface{}, where ...interface{}) error {
	return d.M.Struct(pointer, where...)
}

// Structs retrieves records from table and converts them into given struct slice.
// The parameter <pointer> should be type of *[]struct/*[]*struct. It can create and fill the struct
// slice internally during converting.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved with the given conditions
// from table and <pointer> is not empty.
//
// Eg:
// users := ([]User)(nil)
// err   := dao.User.Structs(&users)
//
// users := ([]*User)(nil)
// err   := dao.User.Structs(&users)
func (d *XsChatroomModuleFactoryDao) Structs(pointer interface{}, where ...interface{}) error {
	return d.M.Structs(pointer, where...)
}

// Scan automatically calls Struct or Structs function according to the type of parameter <pointer>.
// It calls function Struct if <pointer> is type of *struct/**struct.
// It calls function Structs if <pointer> is type of *[]struct/*[]*struct.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved and given pointer is not empty or nil.
//
// Eg:
// user  := new(User)
// err   := dao.User.Where("id", 1).Scan(user)
//
// user  := (*User)(nil)
// err   := dao.User.Where("id", 1).Scan(&user)
//
// users := ([]User)(nil)
// err   := dao.User.Scan(&users)
//
// users := ([]*User)(nil)
// err   := dao.User.Scan(&users)
func (d *XsChatroomModuleFactoryDao) Scan(pointer interface{}, where ...interface{}) error {
	return d.M.Scan(pointer, where...)
}

// Chunk iterates the table with given size and callback function.
func (d *XsChatroomModuleFactoryDao) Chunk(limit int, callback func(entities []*pb.EntityXsChatroomModuleFactory, err error) bool) {
	d.M.Chunk(limit, func(result gdb.Result, err error) bool {
		var entities []*pb.EntityXsChatroomModuleFactory
		err = result.Structs(&entities)
		if err == sql.ErrNoRows {
			return false
		}
		return callback(entities, err)
	})
}

// LockUpdate sets the lock for update for current operation.
func (d *XsChatroomModuleFactoryDao) LockUpdate() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.LockUpdate()}
}

// LockShared sets the lock in share mode for current operation.
func (d *XsChatroomModuleFactoryDao) LockShared() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.LockShared()}
}

// Unscoped enables/disables the soft deleting feature.
func (d *XsChatroomModuleFactoryDao) Unscoped() *XsChatroomModuleFactoryDao {
	return &XsChatroomModuleFactoryDao{M: d.M.Unscoped()}
}

func (d *XsChatroomModuleFactoryDao) scan(res gdb.Result, in interface{}) error {
	length := res.Len()
	if length == 0 {
		return sql.ErrNoRows
	}
	v := reflect.ValueOf(in).Elem()
	newv := reflect.MakeSlice(v.Type(), 0, length)
	v.Set(newv)
	v.SetLen(length)

	index := 0
	var fieldToTag []string
	for i := 0; i < length; i++ {
		k := v.Type().Elem().Elem()
		newObj := reflect.New(k)
		if i == 0 {
			fieldToTag = d.getFieldTag(newObj)
		}
		err := d.mapping(res[i], newObj, fieldToTag)
		if err != nil {
			return err
		}
		v.Index(index).Set(newObj)
		index++
	}
	v.SetLen(index)
	return nil
}

func (d *XsChatroomModuleFactoryDao) getFieldTag(v reflect.Value) []string {
	t := v.Type()
	typ := t.Elem()
	val := v.Elem()
	vTypeOfT := val.Type()
	length := val.NumField()
	fieldToTag := make([]string, length)
	for j := 0; j < length; j++ {
		name := vTypeOfT.Field(j).Name
		if name[0] < 'A' || name[0] > 'Z' {
			continue
		}
		value := val.Field(j)
		tagVal := typ.Field(j).Tag.Get("orm")
		if !value.CanSet() || len(tagVal) == 0 {
			continue
		}
		index := strings.Index(tagVal, ",")
		if index > -1 {
			fieldToTag[j] = tagVal[0:index]
		} else {
			fieldToTag[j] = tagVal
		}
	}
	return fieldToTag
}

func (d *XsChatroomModuleFactoryDao) mapping(m map[string]*gvar.Var, v reflect.Value, fieldToTag []string) error {
	val := v.Elem()
	length := val.NumField()
	for i := 0; i < length; i++ {
		tag := fieldToTag[i]
		if len(tag) == 0 {
			continue
		}

		meta, ok := m[tag]
		if !ok {
			continue
		}
		value := val.Field(i)
		kind := value.Kind()
		switch kind {
		case reflect.String:
			value.SetString(meta.String())
			break
		case reflect.Float32, reflect.Float64:
			value.SetFloat(meta.Float64())
			break
		case reflect.Int64, reflect.Int, reflect.Int8, reflect.Int16, reflect.Int32:
			value.SetInt(meta.Int64())
			break
		case reflect.Uint64, reflect.Uint, reflect.Uint8, reflect.Uint16, reflect.Uint32:
			value.SetUint(meta.Uint64())
			break
		case reflect.Bool:
			value.SetBool(meta.Bool())
			break
		default:
			return errors.New("unknown kind " + kind.String())
			break
		}
	}
	return nil
}
