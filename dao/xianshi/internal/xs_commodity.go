// ==========================================================================
// This is auto-generated by gf cli tool. DO NOT EDIT THIS FILE MANUALLY.
// ==========================================================================

package internal

import (
	"context"
	"database/sql"
	"errors"
	"reflect"
	"strings"
	"time"

	"github.com/gogf/gf/container/gvar"
	"github.com/gogf/gf/database/gdb"
	"github.com/gogf/gf/frame/g"
	"github.com/gogf/gf/frame/gmvc"
	"github.com/gogf/gf/util/gconv"

	//	"banban/app/model"
	"banban/app/pb"
)

// XsCommodityDao is the manager for logic model data accessing
// and custom defined data operations functions management.
type XsCommodityDao struct {
	gmvc.M
	DB      gdb.DB
	Table   string
	Columns xsCommodityColumns
}

// XsCommodityColumns defines and stores column names for table xs_commodity.
type xsCommodityColumns struct {
	Cid            string //
	Name           string //
	GroupID        string // 同组sku物品共属的组id
	NameZhTw       string // 台湾版名称
	NameEn         string // 英文版名称
	Type           string // 经验值（exp）,箱子（box),钥匙(key),头像框(header),爵位(title),礼物(gift),守护(defend),电台守护(radio-defend),优惠券(coupon),坐骑(mounts),ring), 入场特效(effect), 主页装扮(decorate)，联盟宝箱(union-box)，联盟头像框(union_header)，结婚戒指(marry_ring), 游戏赛事(game_ticket), 云游戏时长卡(cloud_game_card), 车队(vehicle_ticket), 陪玩车票(play_ticket),团战应援券(group_pk_coupon),夜店酒水(pub_drink),喊话卡(broadcast_ticket),游戏讲座(game_lecture_video),访客记录本(visitor_book),陪玩券(god_discount_ticket),道具类(prop),小喇叭(trumpet),皮队友通用物品类型(prop_mate),伴伴玩伴卡(game_card),聊天气泡尾饰(bubble_tail),房间资料卡装饰(card_decorate)
	SubType        string // 子类型名
	Description    string // 物品描述
	Price          string // 价格
	Image          string // 图标地址
	ImageBg        string // 背景图
	ImageZhTw      string // 图标地址台湾版
	ImageEn        string // 图标地址英文版
	Period         string // 有效期，精确到天
	PeriodHour     string //
	OnlyNewpay     string // 是否必须为新支付
	DuctionMoney   string // 优惠券抵扣的钱，单位分
	ExtID          string // 类型为礼物优惠券时可使用的物品id 类型为守护、电台守护、爵位优惠券时表示物品的等级 类型为爵位时表示爵位的等级 类型为守护、电台守护时表示守护的等级 类型为礼物时表示礼物的gid
	ExtName        string // 优惠券适用的物品名称
	JumpPage       string // 可跳转的页面
	CouponType     string // 优惠券的类型
	Dateline       string // 创建时间
	IsContinue     string // 是否续费才能用
	CanOpenedByBox string // 头像框、坐骑能否在开箱子中开出来
	SalingOnShop   string // 0非售卖,1商城售卖,2金币商城,3碎片商城,4联盟银币商城,5联盟特权商城,6积分商城
	CanGive        string // 物品是否能够赠送 1代表可以 0代表不可以 默认可以赠送
	Title          string // 最低爵位等级
	ExtIDMore      string //
	Extra          string // 配置信息
	MoneyType      string //
	Startline      string // 开始使用时间
	Endline        string // 结束使用时间
	TagIDs         string // 物品标签,多个id以应文:隔开,如 1:2
	SpecialType    string // 是否特殊礼物，0：否，1：直播粉丝团礼物
	AppID          string // 限定app_id
	Discount       string // 折扣比例
	SubEnable      string // 子APP启用状态，0-都展示，1-仅子APP展示，2-子APP不展示
}

var (
	// XsCommodity is globally public accessible object for table xs_commodity operations.
	XsCommodity = XsCommodityDao{
		M:     g.DB("default").Model("xs_commodity").Safe(),
		DB:    g.DB("default"),
		Table: "xs_commodity",
		Columns: xsCommodityColumns{
			Cid:            "cid",
			Name:           "name",
			GroupID:        "group_id",
			NameZhTw:       "name_zh_tw",
			NameEn:         "name_en",
			Type:           "type",
			SubType:        "sub_type",
			Description:    "description",
			Price:          "price",
			Image:          "image",
			ImageBg:        "image_bg",
			ImageZhTw:      "image_zh_tw",
			ImageEn:        "image_en",
			Period:         "period",
			PeriodHour:     "period_hour",
			OnlyNewpay:     "only_newpay",
			DuctionMoney:   "duction_money",
			ExtID:          "ext_id",
			ExtName:        "ext_name",
			JumpPage:       "jump_page",
			CouponType:     "coupon_type",
			Dateline:       "dateline",
			IsContinue:     "is_continue",
			CanOpenedByBox: "can_opened_by_box",
			SalingOnShop:   "saling_on_shop",
			CanGive:        "can_give",
			Title:          "title",
			ExtIDMore:      "ext_id_more",
			Extra:          "extra",
			MoneyType:      "money_type",
			Startline:      "startline",
			Endline:        "endline",
			TagIDs:         "tag_ids",
			SpecialType:    "special_type",
			AppID:          "app_id",
			Discount:       "discount",
			SubEnable:      "sub_enable",
		},
	}
)

// Ctx is a chaining function, which creates and returns a new DB that is a shallow copy
// of current DB object and with given context in it.
// Note that this returned DB object can be used only once, so do not assign it to
// a global or package variable for long using.
func (d *XsCommodityDao) Ctx(ctx context.Context) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Ctx(ctx)}
}

// As sets an alias name for current table.
func (d *XsCommodityDao) As(as string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.As(as)}
}

// TX sets the transaction for current operation.
func (d *XsCommodityDao) TX(tx *gdb.TX) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.TX(tx)}
}

// Master marks the following operation on master node.
func (d *XsCommodityDao) Master() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Master()}
}

// Slave marks the following operation on slave node.
// Note that it makes sense only if there's any slave node configured.
func (d *XsCommodityDao) Slave() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Slave()}
}

// Args sets custom arguments for model operation.
func (d *XsCommodityDao) Args(args ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Args(args...)}
}

// LeftJoin does "LEFT JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").LeftJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").LeftJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsCommodityDao) LeftJoin(table ...string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.LeftJoin(table...)}
}

// RightJoin does "RIGHT JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").RightJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").RightJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsCommodityDao) RightJoin(table ...string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.RightJoin(table...)}
}

// InnerJoin does "INNER JOIN ... ON ..." statement on the model.
// The parameter <table> can be joined table and its joined condition,
// and also with its alias name, like:
// Table("user").InnerJoin("user_detail", "user_detail.uid=user.uid")
// Table("user", "u").InnerJoin("user_detail", "ud", "ud.uid=u.uid")
func (d *XsCommodityDao) InnerJoin(table ...string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.InnerJoin(table...)}
}

// Fields sets the operation fields of the model, multiple fields joined using char ','.
// The parameter <fieldNamesOrMapStruct> can be type of string/map/*map/struct/*struct.
func (d *XsCommodityDao) Fields(fieldNamesOrMapStruct ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Fields(fieldNamesOrMapStruct...)}
}

// FieldsEx sets the excluded operation fields of the model, multiple fields joined using char ','.
// The parameter <fieldNamesOrMapStruct> can be type of string/map/*map/struct/*struct.
func (d *XsCommodityDao) FieldsEx(fieldNamesOrMapStruct ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.FieldsEx(fieldNamesOrMapStruct...)}
}

// Option sets the extra operation option for the model.
func (d *XsCommodityDao) Option(option int) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Option(option)}
}

// OmitEmpty sets OPTION_OMITEMPTY option for the model, which automatically filers
// the data and where attributes for empty values.
func (d *XsCommodityDao) OmitEmpty() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.OmitEmpty()}
}

// Filter marks filtering the fields which does not exist in the fields of the operated table.
func (d *XsCommodityDao) Filter() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Filter()}
}

// Where sets the condition statement for the model. The parameter <where> can be type of
// string/map/gmap/slice/struct/*struct, etc. Note that, if it's called more than one times,
// multiple conditions will be joined into where statement using "AND".
// Eg:
// Where("uid=10000")
// Where("uid", 10000)
// Where("money>? AND name like ?", 99999, "vip_%")
// Where("uid", 1).Where("name", "john")
// Where("status IN (?)", g.Slice{1,2,3})
// Where("age IN(?,?)", 18, 50)
// Where(User{ Id : 1, UserName : "john"})
func (d *XsCommodityDao) Where(where interface{}, args ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Where(where, args...)}
}

// WherePri does the same logic as M.Where except that if the parameter <where>
// is a single condition like int/string/float/slice, it treats the condition as the primary
// key value. That is, if primary key is "id" and given <where> parameter as "123", the
// WherePri function treats the condition as "id=123", but M.Where treats the condition
// as string "123".
func (d *XsCommodityDao) WherePri(where interface{}, args ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.WherePri(where, args...)}
}

// And adds "AND" condition to the where statement.
func (d *XsCommodityDao) And(where interface{}, args ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.And(where, args...)}
}

// Or adds "OR" condition to the where statement.
func (d *XsCommodityDao) Or(where interface{}, args ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Or(where, args...)}
}

// Group sets the "GROUP BY" statement for the model.
func (d *XsCommodityDao) Group(groupBy string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Group(groupBy)}
}

// Order sets the "ORDER BY" statement for the model.
func (d *XsCommodityDao) Order(orderBy ...string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Order(orderBy...)}
}

// Limit sets the "LIMIT" statement for the model.
// The parameter <limit> can be either one or two number, if passed two number is passed,
// it then sets "LIMIT limit[0],limit[1]" statement for the model, or else it sets "LIMIT limit[0]"
// statement.
func (d *XsCommodityDao) Limit(limit ...int) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Limit(limit...)}
}

// Offset sets the "OFFSET" statement for the model.
// It only makes sense for some databases like SQLServer, PostgreSQL, etc.
func (d *XsCommodityDao) Offset(offset int) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Offset(offset)}
}

// Page sets the paging number for the model.
// The parameter <page> is started from 1 for paging.
// Note that, it differs that the Limit function start from 0 for "LIMIT" statement.
func (d *XsCommodityDao) Page(page, limit int) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Page(page, limit)}
}

// Batch sets the batch operation number for the model.
func (d *XsCommodityDao) Batch(batch int) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Batch(batch)}
}

// Cache sets the cache feature for the model. It caches the result of the sql, which means
// if there's another same sql request, it just reads and returns the result from cache, it
// but not committed and executed into the database.
//
// If the parameter <duration> < 0, which means it clear the cache with given <name>.
// If the parameter <duration> = 0, which means it never expires.
// If the parameter <duration> > 0, which means it expires after <duration>.
//
// The optional parameter <name> is used to bind a name to the cache, which means you can later
// control the cache like changing the <duration> or clearing the cache with specified <name>.
//
// Note that, the cache feature is disabled if the model is operating on a transaction.
func (d *XsCommodityDao) Cache(duration time.Duration, name ...string) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Cache(duration, name...)}
}

// Data sets the operation data for the model.
// The parameter <data> can be type of string/map/gmap/slice/struct/*struct, etc.
// Eg:
// Data("uid=10000")
// Data("uid", 10000)
// Data(g.Map{"uid": 10000, "name":"john"})
// Data(g.Slice{g.Map{"uid": 10000, "name":"john"}, g.Map{"uid": 20000, "name":"smith"})
func (d *XsCommodityDao) Data(data ...interface{}) *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Data(data...)}
}

// All does "SELECT FROM ..." statement for the model.
// It retrieves the records from table and returns the result as []*pb.EntityXsCommodity.
// It returns nil if there's no record retrieved with the given conditions from table.
//
// The optional parameter <where> is the same as the parameter of M.Where function,
// see M.Where.
func (d *XsCommodityDao) All(where ...interface{}) ([]*pb.EntityXsCommodity, error) {
	all, err := d.M.All(where...)
	if err != nil {
		return nil, err
	}
	var entities []*pb.EntityXsCommodity
	//if err = all.Structs(&entities); err != nil && err != sql.ErrNoRows {
	//	return nil, err
	//}
	if err = d.scan(all, &entities); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entities, nil
}

// One retrieves one record from table and returns the result as *pb.EntityXsCommodity.
// It returns nil if there's no record retrieved with the given conditions from table.
//
// The optional parameter <where> is the same as the parameter of M.Where function,
// see M.Where.
func (d *XsCommodityDao) One(where ...interface{}) (*pb.EntityXsCommodity, error) {
	one, err := d.M.One(where...)
	if err != nil {
		return nil, err
	}
	var entity *pb.EntityXsCommodity
	if err = one.Struct(&entity); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entity, nil
}

// FindOne retrieves and returns a single Record by M.WherePri and M.One.
// Also see M.WherePri and M.One.
func (d *XsCommodityDao) FindOne(where ...interface{}) (*pb.EntityXsCommodity, error) {
	one, err := d.M.FindOne(where...)
	if err != nil {
		return nil, err
	}
	var entity *pb.EntityXsCommodity
	if err = one.Struct(&entity); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entity, nil
}

// FindAll retrieves and returns Result by by M.WherePri and M.All.
// Also see M.WherePri and M.All.
func (d *XsCommodityDao) FindAll(where ...interface{}) ([]*pb.EntityXsCommodity, error) {
	all, err := d.M.FindAll(where...)
	if err != nil {
		return nil, err
	}
	var entities []*pb.EntityXsCommodity
	//if err = all.Structs(&entities); err != nil && err != sql.ErrNoRows {
	//	return nil, err
	//}
	if err = d.scan(all, &entities); err != nil && err != sql.ErrNoRows {
		return nil, err
	}
	return entities, nil
}

// Struct retrieves one record from table and converts it into given struct.
// The parameter <pointer> should be type of *struct/**struct. If type **struct is given,
// it can create the struct internally during converting.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved with the given conditions
// from table and <pointer> is not nil.
//
// Eg:
// user := new(User)
// err  := dao.User.Where("id", 1).Struct(user)
//
// user := (*User)(nil)
// err  := dao.User.Where("id", 1).Struct(&user)
func (d *XsCommodityDao) Struct(pointer interface{}, where ...interface{}) error {
	return d.M.Struct(pointer, where...)
}

// Structs retrieves records from table and converts them into given struct slice.
// The parameter <pointer> should be type of *[]struct/*[]*struct. It can create and fill the struct
// slice internally during converting.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved with the given conditions
// from table and <pointer> is not empty.
//
// Eg:
// users := ([]User)(nil)
// err   := dao.User.Structs(&users)
//
// users := ([]*User)(nil)
// err   := dao.User.Structs(&users)
func (d *XsCommodityDao) Structs(pointer interface{}, where ...interface{}) error {
	return d.M.Structs(pointer, where...)
}

// Scan automatically calls Struct or Structs function according to the type of parameter <pointer>.
// It calls function Struct if <pointer> is type of *struct/**struct.
// It calls function Structs if <pointer> is type of *[]struct/*[]*struct.
//
// The optional parameter <where> is the same as the parameter of Model.Where function,
// see Model.Where.
//
// Note that it returns sql.ErrNoRows if there's no record retrieved and given pointer is not empty or nil.
//
// Eg:
// user  := new(User)
// err   := dao.User.Where("id", 1).Scan(user)
//
// user  := (*User)(nil)
// err   := dao.User.Where("id", 1).Scan(&user)
//
// users := ([]User)(nil)
// err   := dao.User.Scan(&users)
//
// users := ([]*User)(nil)
// err   := dao.User.Scan(&users)
func (d *XsCommodityDao) Scan(pointer interface{}, where ...interface{}) error {
	return d.M.Scan(pointer, where...)
}

// Chunk iterates the table with given size and callback function.
func (d *XsCommodityDao) Chunk(limit int, callback func(entities []*pb.EntityXsCommodity, err error) bool) {
	d.M.Chunk(limit, func(result gdb.Result, err error) bool {
		var entities []*pb.EntityXsCommodity
		err = result.Structs(&entities)
		if err == sql.ErrNoRows {
			return false
		}
		return callback(entities, err)
	})
}

// LockUpdate sets the lock for update for current operation.
func (d *XsCommodityDao) LockUpdate() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.LockUpdate()}
}

// LockShared sets the lock in share mode for current operation.
func (d *XsCommodityDao) LockShared() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.LockShared()}
}

// Unscoped enables/disables the soft deleting feature.
func (d *XsCommodityDao) Unscoped() *XsCommodityDao {
	return &XsCommodityDao{M: d.M.Unscoped()}
}

func (d *XsCommodityDao) scan(res gdb.Result, in interface{}) error {
	length := res.Len()
	if length == 0 {
		return sql.ErrNoRows
	}
	v := reflect.ValueOf(in).Elem()
	newv := reflect.MakeSlice(v.Type(), 0, length)
	v.Set(newv)
	v.SetLen(length)

	index := 0
	var fieldToTag []string
	for i := 0; i < length; i++ {
		k := v.Type().Elem().Elem()
		newObj := reflect.New(k)
		if i == 0 {
			fieldToTag = d.getFieldTag(newObj)
		}
		err := d.mapping(res[i], newObj, fieldToTag)
		if err != nil {
			return err
		}
		v.Index(index).Set(newObj)
		index++
	}
	v.SetLen(index)
	return nil
}

func (d *XsCommodityDao) getFieldTag(v reflect.Value) []string {
	t := v.Type()
	typ := t.Elem()
	val := v.Elem()
	vTypeOfT := val.Type()
	length := val.NumField()
	fieldToTag := make([]string, length)
	for j := 0; j < length; j++ {
		name := vTypeOfT.Field(j).Name
		if name[0] < 'A' || name[0] > 'Z' {
			continue
		}
		value := val.Field(j)
		tagVal := typ.Field(j).Tag.Get("orm")
		if !value.CanSet() || len(tagVal) == 0 {
			continue
		}
		index := strings.Index(tagVal, ",")
		if index > -1 {
			fieldToTag[j] = tagVal[0:index]
		} else {
			fieldToTag[j] = tagVal
		}
	}
	return fieldToTag
}

func (d *XsCommodityDao) mapping(m map[string]*gvar.Var, v reflect.Value, fieldToTag []string) error {
	val := v.Elem()
	length := val.NumField()
	for i := 0; i < length; i++ {
		tag := fieldToTag[i]
		if len(tag) == 0 {
			continue
		}

		meta, ok := m[tag]
		if !ok {
			continue
		}
		value := val.Field(i)
		kind := value.Kind()
		switch kind {
		case reflect.String:
			value.SetString(meta.String())
			break
		case reflect.Float32, reflect.Float64:
			value.SetFloat(gconv.Float64(meta.String()))
			break
		case reflect.Int64, reflect.Int, reflect.Int8, reflect.Int16, reflect.Int32:
			value.SetInt(meta.Int64())
			break
		case reflect.Uint64, reflect.Uint, reflect.Uint8, reflect.Uint16, reflect.Uint32:
			value.SetUint(meta.Uint64())
			break
		case reflect.Bool:
			value.SetBool(meta.Bool())
			break
		default:
			return errors.New("unknown kind " + kind.String())
			break
		}
	}
	return nil
}
